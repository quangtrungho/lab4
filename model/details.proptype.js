import PropTypes from "prop-types";
import tabletPropType from "./tablet.proptype";

export default detailsPropType = {
  value: tabletPropType,
  onchangeValue: PropTypes.func,
  handleSave: PropTypes.func,
};
